// const BASE_URL = "mongodb://localhost/final-project/register"

// export const API_URL = (slug) => `${API_URL}/${slug}`

export const EVENT_URL = (mode) => {
  return `https://final-project-eventapp.herokuapp.com/register${mode}`
}